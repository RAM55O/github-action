name: Hello World Concurrent

on:
  push:
    branches :
      - main
      - develop
      - 'release/**'
  pull_request:
    types:
      - opened
    branches:
      - main



jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello Step
        run: |
          echo "one"
          echo "two"
  hello1:
    runs-on: ubuntu-latest
    needs: hello

    steps:
      - name: Hello Step
        run: |
          echo "hellow , ${{vars.my}}"
          echo "one"
          echo "two"
  hello3:
    runs-on: ubuntu-latest
    needs: hello

    steps:
      - name: Hello Step
        run: |
          echo "one"
          echo "two"          
